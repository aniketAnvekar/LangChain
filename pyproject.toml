[project]
name = "langchain-tutorial"
version = "0.1.0"
description = "LangChain Tutorial with Claude"
requires-python = ">=3.9"
dependencies = [
    "langchain",
    "langchain-core",
    "langchain-anthropic",
    "langchain-ollama",
    "python-dotenv",
]

[tool.uv]
